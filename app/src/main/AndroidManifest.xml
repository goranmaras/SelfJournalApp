<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.goranm.self">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="util.JournalApi"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_new_realse"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_new_realse_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".JournalListActivity"></activity>
        <activity android:name=".PostJournalActivity" />
        <activity android:name=".CreateAccountActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713"/>
    </application>

</manifest>